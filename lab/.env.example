# Copy to .env only for local runs. Never commit .env.
# Preferred: source ~/agent-webmcp/.env.local instead (already has all keys).
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
# Text helper (Mercury): Inception direct. Nous key is out of balance.
INCEPTION_API_KEY=
TEXT_MODEL_API_KEY=
TEXT_MODEL_BASE_URL=https://api.inceptionlabs.ai/v1
TEXT_MODEL=mercury-2.5
